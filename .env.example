# Server port for the web UI and API
PORT=11000

# Telegram API credentials (get from https://my.telegram.org)
API_ID=123456
API_HASH=your_api_hash_here

# Target chat ID where files will be uploaded.
# Example: for your Saved Messages use your user ID; for a channel/group use its ID.
# Accepts numeric ID (recommended). Do NOT quote.
TARGET_CHATID=123456789

# Web UI basic auth credentials
UI_USERNAME=your_username
UI_PASSWORD=your_password

# Notes:
# - Create a .env in the project root (same folder as this file) and copy these keys with real values.
# - .env is intentionally gitignored; do not commit secrets.